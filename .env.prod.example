# Copy to `.env` ON THE BOX and fill in. Never commit it.
#
# Read by docker compose (docker-compose.yml + docker-compose.prod.yml), by deploy.sh and by
# scripts/backup-db.sh. Every one of these is required: the backend throws on a missing variable
# rather than booting broken (backend/.../config/Env.kt), and compose refuses to start without them.
#
# deploy.sh SOURCES this file, so bash parses it. QUOTE any value containing a space or a shell
# metacharacter. Unquoted, the AUTH_EMAIL_FROM below read as a redirection and the first real deploy
# died on `syntax error near unexpected token` naming only a line number. Compose strips the quotes.

# --- the two hostnames -------------------------------------------------------------------------
# Where the API lives. certbot asks Let's Encrypt for a certificate for exactly this name, so its A
# record must already point at the box.
API_DOMAIN=api.sharpeyes.app

# Where the frontend lives (Vercel). This is the ONLY origin the backend's CORS will accept.
# Note: allowHost strips the scheme and matches host AND port, so include a port if there is one.
FRONTEND_ORIGIN=https://sharpeyes.app

# Required to register the ACME account, and that is ALL it is now. Let's Encrypt ended expiration
# notification emails on 2025-06-04 and no longer stores the address: what you put here is forwarded
# to an ISRG mailing list, unattached to the account. Confirmed against the live account, which comes
# back with no `contact` field at all while `certbot update_account` still prints that it updated it.
#
# So this address will NOT warn you about anything. A renewal that silently stops working takes the
# site down 90 days later and nothing emails you about it. Watch the certificate yourself.
CERTBOT_EMAIL=

# --- database ----------------------------------------------------------------------------------
# Postgres runs as a container on this box. DB_HOST/DB_PORT/DB_NAME come from docker-compose.yml
# and do not belong here; only the credentials do.
#
# Generate: openssl rand -base64 32
DB_USERNAME=sharpeyes
DB_PASSWORD=

# --- auth --------------------------------------------------------------------------------------
# The signing keys in auth_jwks are encrypted with this. Change it and every existing key becomes
# unreadable: the service mints a new one and every token in flight fails at once. Back it up with
# the database, or a restore is a database nobody can sign in to.
#
# Generate: openssl rand -base64 32
AUTH_SECRET=

# The session cookie's scope. A LEADING DOT and the parent domain, so the frontend on the apex and
# the auth service on api. share one session. A bare subdomain here means the apex never gets it,
# and sign-in appears to do nothing at all.
AUTH_COOKIE_DOMAIN=.sharpeyes.app

# --- Discord -----------------------------------------------------------------------------------
# https://discord.com/developers/applications -> your application -> OAuth2.
#
# The redirect URI registered there must be exactly:
#   https://api.sharpeyes.app/api/auth/callback/discord
# Discord matches it character for character and refuses anything else, including a trailing slash.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# --- email and password (OFF, and nothing below is needed while it is) --------------------------
# Discord is the only way in until this is true. Leave it alone for the first deploy.
#
# Turning it on later is this line plus the two below, then `./deploy.sh`. No code change and no
# migration: V66 already carries the columns. Nobody who signed up with Discord is stranded either,
# "forgot password" CREATES a password on an account that has none.
AUTH_PASSWORD_LOGIN=false

# Required only when the line above is true, and then genuinely required: the service refuses to
# start without a sender, because a password nobody can reset is an account that can be lost for
# good. From https://resend.com, verify sharpeyes.app as a sending domain (one DNS record), then
# create an API key. Free tier is 3k emails a month.
RESEND_API_KEY=
AUTH_EMAIL_FROM="SharpEyes <noreply@sharpeyes.app>"

# --- backups -----------------------------------------------------------------------------------
# From `terraform output` in infra/. The key is PutObject-only and scoped to this one bucket, so it
# can neither read nor delete what it writes.
BACKUP_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# --- the test account ----------------------------------------------------------------------------
# The ONE account scripts/wipe-account.sh may erase, so onboarding can be walked again from a clean
# start. Optional, and empty means that script can wipe nothing at all. Not in the repo because this
# is a real person's address and the repo is public.
WIPE_ACCOUNT_EMAIL=
